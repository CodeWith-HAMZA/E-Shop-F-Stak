import ShoppingCart from "@/components/client/ShoppingCart";
import React, { Fragment } from "react";

const page = () => {
  return (
    <Fragment>
      <ShoppingCart />
    </Fragment>
  );
};

export default page;
